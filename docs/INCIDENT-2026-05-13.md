# INCIDENT 2026-05-13 — SERVEUR GNAMBA

## Contexte
Le serveur Gnamba est dans un état dégradé car :

- Backups Supabase cassés depuis 3 jours
- Supabase local arrêté
- 3 migrations critiques désactivées
- Configuration fragmentée sur 8 fichiers `.env`
- 9.2M d’archives et builds obsolètes
- Les conteneurs Docker EGS restent en ligne

## Objectif
Restaurer un environnement de développement cohérent, local et cloud, avec sauvegardes fonctionnelles et scripts de contrôle, dans les 48 heures.

## Actions réalisées

1. Correction du mot de passe PostgreSQL dans `.env.server`
2. Ajout de `scripts/fix-password.sh`
3. Ajout de `scripts/test-db-connection.sh`
4. Ajout de `scripts/start-supabase-local.sh`
5. Ajout de `scripts/reactivate-migrations.sh`
6. Ajout de `scripts/apply-migrations.sh`
7. Ajout de `scripts/validate-env.sh`
8. Ajout de `scripts/verify-backup.sh`
9. Ajout de `scripts/audit-rls.sh` et `sql/audit-rls.sql`
10. Ajout de `scripts/cleanup-archives.sh`
11. Ajout de `scripts/gnamba-health.sh`
12. Ajout de `scripts/setup-supabase-autostart.sh`
13. Ajout de `.env.template`
14. Mise à jour de la documentation projet pour mieux encadrer `.env` et Supabase local

## Commandes d’exécution recommandées

### Fix urgent
```bash
bash scripts/fix-password.sh
bash scripts/test-db-connection.sh
```

### Supabase local
```bash
bash scripts/start-supabase-local.sh
```

### Migrations
```bash
bash scripts/reactivate-migrations.sh
bash scripts/apply-migrations.sh
```

### Validation
```bash
bash scripts/validate-env.sh .env
bash scripts/verify-backup.sh
bash scripts/audit-rls.sh
bash scripts/gnamba-health.sh
```

### Nettoyage
```bash
bash scripts/cleanup-archives.sh
```

### Démarrage automatique sur reboot
```bash
sudo bash scripts/setup-supabase-autostart.sh
```

## Critères d’acceptation

- `bash scripts/test-db-connection.sh` retourne `✅ OK`
- `bash scripts/start-supabase-local.sh` signale tous les ports 54321/54322/54323/54324
- `bash scripts/apply-migrations.sh` applique les migrations après réactivation
- `bash scripts/validate-env.sh .env` ne retourne aucune erreur
- `bash scripts/verify-backup.sh` trouve et vérifie un backup valide
- `bash scripts/audit-rls.sh` s’exécute sans erreur
- `bash scripts/gnamba-health.sh` exit 0 et affiche des ✔️

## Remarques

- Les scripts sont conçus pour être exécutés depuis `/home/soma/gnamba-project/`.
- La configuration centralisée est maintenant `.env.template`.
- Les fichiers de build temporaires doivent être nettoyés pour restaurer la lisibilité du repository.
